'use strict';

const mongoose = require('mongoose');

const historySchema = mongoose.Schema({
  action : {type:'string'},
  payload : {type:'string'}
});

module.exports = mongoose.model('note', historySchema);